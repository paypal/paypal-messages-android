name: 'Publish to Central Portal'
description: 'Publishes all modules using the Sonatype Central Portal API'
inputs:
  sonatype_user:
    description: 'Sonatype user'
    required: true
    default: ''
  sonatype_token:
    description: 'Sonatype user token'
    required: true
    default: ''
  signing_key_id:
    description: 'Signing key id'
    required: true
    default: ''
  signing_key_password:
    description: 'Signing key password'
    required: true
    default: ''
  signing_key_file:
    description: 'Signing key file'
    required: true
    default: ''
  auto_publish:
    description: 'Whether to publish automatically'
    required: false
    default: 'false'
runs:
  using: "composite"
  steps:
    - run: |
        echo "::debug::Using Sonatype credentials from secrets"
        
        # First publish library module which will handle signing
        ./gradlew --stacktrace :library:assemble
        ./gradlew --stacktrace :library:signArchives
        
        # Then run the central portal publishing with sonatypePassword property
        if [ "${{ inputs.auto_publish }}" = "true" ]; then
          ./gradlew --stacktrace publishToCentralPortal -PautoPublish -PsonatypeUsername="${{ inputs.sonatype_user }}" -PsonatypePassword="${{ inputs.sonatype_token }}"
        else
          ./gradlew --stacktrace publishToCentralPortal -PsonatypeUsername="${{ inputs.sonatype_user }}" -PsonatypePassword="${{ inputs.sonatype_token }}"
        fi
        ./gradlew --stacktrace checkCentralPortalDeployment -PsonatypeUsername="${{ inputs.sonatype_user }}" -PsonatypePassword="${{ inputs.sonatype_token }}"
      shell: bash
      env:
        # Set both environment variables and pass as properties for redundancy
        SONATYPE_NEXUS_USERNAME: ${{ inputs.sonatype_user }}
        SONATYPE_NEXUS_PASSWORD: ${{ inputs.sonatype_token }}
        SIGNING_KEY_ID: ${{ inputs.signing_key_id }}
        SIGNING_KEY_PASSWORD: ${{ inputs.signing_key_password }}
        SIGNING_KEY_FILE: ${{ inputs.signing_key_file }}