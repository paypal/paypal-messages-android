name: 'Publish to Maven Central'
description: 'Publishes all modules using the Maven Central Portal Plugin'
inputs:
  sonatype_user:
    description: 'Sonatype user'
    required: true
    default: ''
  sonatype_token:
    description: 'Sonatype user token'
    required: true
    default: ''
  signing_key_id:
    description: 'Signing key id'
    required: true
    default: ''
  signing_key_password:
    description: 'Signing key password'
    required: true
    default: ''
  signing_key_file:
    description: 'Signing key file'
    required: true
    default: ''
  auto_publish:
    description: 'Whether to publish automatically'
    required: false
    default: 'true'
  use_snapshot:
    description: 'Whether to publish as a SNAPSHOT version'
    required: false
    default: 'false'
runs:
  using: "composite"
  steps:
    - name: Set up Maven
      uses: stCarolas/setup-maven@v4.5
      with:
        maven-version: 3.9.5

    - name: Create Maven settings directory
      shell: bash
      run: |
        mkdir -p ${{ github.workspace }}/.mvn

    - name: Create Maven settings file
      shell: bash
      run: |
        cat > ${{ github.workspace }}/.mvn/maven-settings.xml << 'EOF'
        <?xml version="1.0" encoding="UTF-8"?>
        <settings xmlns="http://maven.apache.org/SETTINGS/1.0.0"
                  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                  xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.0.0 http://maven.apache.org/xsd/settings-1.0.0.xsd">
            <servers>
                <server>
                    <id>central</id>
                    <username>${env.SONATYPE_NEXUS_USERNAME}</username>
                    <password>${env.SONATYPE_NEXUS_PASSWORD}</password>
                </server>
            </servers>
            <profiles>
                <profile>
                    <id>gpg</id>
                    <properties>
                        <gpg.executable>gpg</gpg.executable>
                        <gpg.keyname>${env.SIGNING_KEY_ID}</gpg.keyname>
                        <gpg.passphrase>${env.SIGNING_KEY_PASSWORD}</gpg.passphrase>
                    </properties>
                </profile>
            </profiles>
            <activeProfiles>
                <activeProfile>gpg</activeProfile>
            </activeProfiles>
        </settings>
        EOF

    - name: Prepare and publish via NMCP (library only)
      shell: bash
      env:
        SONATYPE_NEXUS_USERNAME: ${{ inputs.sonatype_user }}
        SONATYPE_NEXUS_PASSWORD: ${{ inputs.sonatype_token }}
        SIGNING_KEY_ID: ${{ inputs.signing_key_id }}
        SIGNING_KEY_PASSWORD: ${{ inputs.signing_key_password }}
        SIGNING_KEY_FILE: ${{ inputs.signing_key_file }}
        USE_SNAPSHOT: ${{ inputs.use_snapshot }}
        AUTO_PUBLISH: ${{ inputs.auto_publish }}
      run: |
         echo "Publishing via NMCP module plugin (:library)"
 
         if [ ! -d "${{ github.workspace }}/library" ]; then
           echo "Error: library directory not found"
           exit 1
         fi
 
         VERSION=$(grep -o '"sdkVersionName"\s*:\s*"[^"]*"' ${{ github.workspace }}/build.gradle | grep -o '"[^"]*"$' | tr -d '"')
 
         if [ "$USE_SNAPSHOT" = "true" ]; then
           [[ "$VERSION" != *-SNAPSHOT ]] && VERSION="${VERSION}-SNAPSHOT"
           echo "Publishing SNAPSHOT version: $VERSION"
         else
           [[ "$VERSION" == *-SNAPSHOT ]] && VERSION="${VERSION%-SNAPSHOT}"
           echo "Publishing release version: $VERSION"
         fi
 
         echo "Importing GPG key for signing"
         gpg --batch --import ${{ inputs.signing_key_file }}
 
         echo "Setting project version to ${VERSION}"
         ./gradlew -PversionParam="$VERSION" changeReleaseVersion --no-daemon --stacktrace
 
         echo "Cleaning and building artifacts..."
         ./gradlew clean :library:assembleRelease :library:generatePomFileForReleasePublication --no-daemon --stacktrace
 
        echo "Injecting NMCP Aggregation plugin into root build.gradle"
        cp ${{ github.workspace }}/build.gradle ${{ github.workspace }}/build.gradle.backup

        # Insert aggregation plugin id into plugins block of root
        cat > ${{ github.workspace }}/inject-nmcp-agg.awk << 'EOF'
        /^plugins \{/ {
          print $0
          print "\tid 'com.gradleup.nmcp.aggregation' version '1.0.3'"
          next
        }
        { print }
        EOF
        awk -f ${{ github.workspace }}/inject-nmcp-agg.awk ${{ github.workspace }}/build.gradle.backup > ${{ github.workspace }}/build.gradle

        # Append nmcpAggregation configuration to root
        cat > ${{ github.workspace }}/root-nmcp-agg-config.gradle << 'EOF'
        nmcpAggregation {
          centralPortal {
            username = System.getenv('SONATYPE_NEXUS_USERNAME') ?: (project.findProperty('sonatypeUsername') ?: '')
            password = System.getenv('SONATYPE_NEXUS_PASSWORD') ?: (project.findProperty('sonatypePassword') ?: '')
            publishingType = System.getenv('AUTO_PUBLISH') == 'true' ? 'AUTOMATIC' : 'USER_MANAGED'
          }
          // Publish the library module
          publishAllProjectsProbablyBreakingProjectIsolation()
        }
        EOF
        cat ${{ github.workspace }}/root-nmcp-agg-config.gradle >> ${{ github.workspace }}/build.gradle

        echo "Detecting nmcp aggregation publish task in root"
        ./gradlew -q tasks --all | cat

        if [ "$USE_SNAPSHOT" = "true" ]; then
          AGG_TASK="publishAggregationToCentralPortalSnapshots"
        else
          AGG_TASK="publishAggregationToCentralPortal"
        fi

        if ! ./gradlew -q tasks --all | grep -q "${AGG_TASK}"; then
          echo "❌ ERROR: ${AGG_TASK} not found. Available tasks:" && ./gradlew -q tasks --all | cat
          exit 1
        fi

        echo "Publishing with ${AGG_TASK}"
        ./gradlew ${AGG_TASK} \
          -Psigning.keyId="${SIGNING_KEY_ID}" \
          -Psigning.password="${SIGNING_KEY_PASSWORD}" \
          -Psigning.secretKeyRingFile="${SIGNING_KEY_FILE}" \
          --no-daemon --stacktrace

        echo "Restoring root build.gradle"
        mv ${{ github.workspace }}/build.gradle.backup ${{ github.workspace }}/build.gradle
        rm -f ${{ github.workspace }}/inject-nmcp-agg.awk ${{ github.workspace }}/root-nmcp-agg-config.gradle
 
         echo "======================================================================"
         echo "NMCP publish finished. Check Sonatype Central Portal for status:"
         echo "https://central.sonatype.com/publishing/deployments"
         echo "======================================================================"